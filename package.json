{
  "name": "claude-mileage",
  "version": "0.1.0",
  "description": "Local-first preparation layer for Claude CLI",
  "type": "module",
  "files": [
    "dist",
    "public/token-tengu-banner.png",
    "README.md",
    "LICENSE"
  ],
  "bin": {
    "claude-mileage": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "test": "npm run build && node test/reliability.test.js && node test/prompt.test.js",
    "start": "node dist/index.js",
    "prepublishOnly": "npm run build"
  },
  "keywords": [
    "claude",
    "anthropic",
    "cli",
    "workflow",
    "context"
  ],
  "license": "MIT",
  "dependencies": {
    "commander": "^12.0.0",
    "chalk": "^5.3.0",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "@types/node": "^20.11.0"
  }
}
